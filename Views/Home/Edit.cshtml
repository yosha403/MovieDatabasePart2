@model Movie
<h2>Edit @Model.Title</h2>
<p>To setup data annotations on a form you need 2 things: </p>
<ul>
    <li>To import the desired model at the top of the page</li>
    <li>You need to import the built Javascript Validation file, which comes with .Net </li>
</ul>
<form asp-action="Edit" method="post">
    <label asp-for="Id" class="control-label"></label>
    <input asp-for="Id" class="form-control" value="@Model.Id" disabled />
    <label asp-for="Title" class="control-label"></label>
    <input asp-for="Title" class="form-control" value="@Model.Title" />
    <span asp-validation-for="Title" class="text-danger"></span>
    <label asp-for="Year" class="control-label"></label>
    <input asp-for="Year" class="form-control" value="@Model.Year" />
    <span asp-validation-for="Year" class="text-danger"></span>
    <label asp-for="RunTime" class="control-label"></label>
    <input asp-for="RunTime" class="form-control" value="@Model.RunTime" />
    <span asp-validation-for="RunTime" class="text-danger"></span>
    <label asp-for="Genre" class="control-label"></label>
    <input asp-for="Genre" class="form-control" value="@Model.Genre" />
    <span asp-validation-for="Genre" class="text-danger"></span>
    <!--You still have access to all of your usual HTML elements with data annotations-->
    <input type="submit" />
</form>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
